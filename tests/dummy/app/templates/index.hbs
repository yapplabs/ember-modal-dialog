<h1 id="title">Basic examples of the <code>modal-dialog</code> component</h1>
<p>
  This component uses <code>ember-wormhole</code> to relocate the dialog in the
  DOM to better sit in a layer above the rest of your document.
</p>
<p>
  It requires no additional dependencies because <code>ember-wormhole</code> is included with
  this addon. If <code>liquid-wormhole</code> is installed, this component will detect it,
  and will become {{#link-to 'animatable'}}animatable{{/link-to}} if
  <code>animatable=true</code> is passed.
</p>

<div class='example' id='example-basic'>
<h2>Basic</h2>
  <button onclick={{action (mut isShowingBasic) true}}>Do It</button>
  {{code-snippet name='basic-modal-dialog.hbs'}}
  {{#if isShowingBasic}}
    {{!-- BEGIN-SNIPPET basic-modal-dialog --}}
    {{#modal-dialog
        onClose=(action (mut isShowingBasic) false)
        aria-labelledby="example-basic-title"
        aria-describedby="example-basic-desc"
    }}
      <h1 id="example-basic-title">Stop! Modal Time!</h1>
      <p id="example-basic-desc">Basic</p>
      <button onclick={{action (mut isShowingBasic) false}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-translucent'>
  <h2>With Translucent Overlay</h2>
  <button onclick={{action (mut isShowingTranslucent) true}}>Do It</button>
  {{code-snippet name='translucent-modal-dialog.hbs'}}
  {{#if isShowingTranslucent}}
    {{!-- BEGIN-SNIPPET translucent-modal-dialog --}}
    {{#modal-dialog
        onClose=(action (mut isShowingTranslucent) false)
        translucentOverlay=true
        aria-labelledby="example-translucent-title"
        aria-describedby="example-translucent-desc"
    }}
      <h1 id="example-translucent-title">Stop! Modal Time!</h1>
      <p id="example-translucent-desc">With Translucent Overlay</p>
      <button onclick={{action (mut isShowingTranslucent) false}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-translucent-with-callback'>
  <h2>Translucent Overlay with Callback</h2>
  <button onclick={{action (mut isShowingTranslucentWithCallback) true}}>Do It</button>
  {{code-snippet name='translucent-modal-dialog-with-callback.hbs'}}
  {{#if isShowingTranslucentWithCallback}}
    {{!-- BEGIN-SNIPPET translucent-modal-dialog-with-callback --}}
    {{#modal-dialog
        onClose=(action (mut isShowingTranslucentWithCallback) false)
        translucentOverlay=true
        onClickOverlay='clickedTranslucentOverlay'
        aria-labelledby="example-translucent-with-callback-title"
        aria-describedby="example-translucent-with-callback-desc"
    }}
      <h1 id="example-translucent-with-callback-title">Stop! Modal Time!</h1>
      <p id="example-translucent-with-callback-desc">Translucent Overlay with Callback</p>
      <button onclick={{action (mut isShowingTranslucentWithCallback) false}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-without-overlay'>
  <h2>Without Overlay</h2>
  <button onclick={{action (mut isShowingWithoutOverlay) true}}>Do It</button>
  {{code-snippet name='modal-dialog-without-overlay.hbs'}}
  {{#if isShowingWithoutOverlay}}
    {{!-- BEGIN-SNIPPET modal-dialog-without-overlay --}}
    {{#modal-dialog
        onClose=(action (mut isShowingWithoutOverlay) false)
        hasOverlay=false
        clickOutsideToClose=true
        aria-labelledby="example-without-overlay-title"
        aria-describedby="example-without-overlay-desc"
    }}
      <h1 id="example-without-overlay-title">Stop! Modal Time!</h1>
      <p id="example-without-overlay-desc">Without Overlay</p>
      <button onclick={{action (mut isShowingWithoutOverlay) false}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-overlay-sibling'>
  <h2>With Overlay as a Sibling in the DOM</h2>
  <button onclick={{action (mut isShowingSibling) true}}>Do It</button>
  {{code-snippet name='translucent-modal-dialog-sibling.hbs'}}
  {{#if isShowingSibling}}
    {{!-- BEGIN-SNIPPET translucent-modal-dialog-sibling --}}
    {{#modal-dialog
        onClose=(action (mut isShowingSibling) false)
        translucentOverlay=true
        overlayPosition='sibling'
        aria-labelledby="example-overlay-sibling-title"
        aria-describedby="example-overlay-sibling-desc"
    }}
      <h1 id="example-overlay-sibling-title">Stop! Modal Time!</h1>
      <p id="example-overlay-sibling-desc">With Translucent Overlay as Sibling</p>
      <button onclick={{action (mut isShowingSibling) false}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-custom-styles'>
  <h2>Custom Styles</h2>
  <button onclick={{action (mut isShowingCustomStyles) true}}>Do It</button>
  {{code-snippet name='custom-styles-modal-dialog.hbs'}}
  {{code-snippet name='custom-styles.scss'}}
  {{#if isShowingCustomStyles}}
    {{!-- BEGIN-SNIPPET custom-styles-modal-dialog --}}
    {{#modal-dialog
      onClose=(action (mut isShowingCustomStyles) false)
        targetAttachment='none'
        containerClass=customContainerClassNames
        overlayClass='custom-styles-overlay'
        aria-labelledby="example-custom-styles-title"
        aria-describedby="example-custom-styles-desc"
    }}
      <h1 id="example-custom-styles-title">Stop! Modal Time!</h1>
      <p id="example-custom-styles-desc">Custom Styles</p>
      <button onclick={{action (mut isShowingCustomStyles) false}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-target-selector'>
  <h2>Target (Selector)</h2>
  <div class='targetContainer'>
    <button id='alignModalDialogToMe' {{action 'toggleTargetSelector'}}>Do It</button>
  </div>
  {{code-snippet name='target-selector-modal-dialog.hbs'}}
  {{#if isShowingTargetSelector}}
    {{!-- BEGIN-SNIPPET target-selector-modal-dialog --}}
    {{#modal-dialog onClose='toggleTargetSelector'
                    targetAttachment=exampleTargetAttachment
                    attachment=exampleAttachment
                    target='#alignModalDialogToMe'
                    aria-labelledby="example-target-selector-title"
                    aria-describedby="example-target-selector-desc" }}
      <h1 id="example-target-selector-title">Stop! Modal Time!</h1>
      <p id="example-target-selector-desc">Target - Selector: '#alignModalDialogToMe'</p>
      <p>Target Attachment: {{exampleTargetAttachment}}</p>
      <p>Attachment: {{exampleAttachment}}</p>
      <button {{action 'closeTargetSelector'}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-target-element'>
  <h2>Target (Element)</h2>
  <div class='targetContainer'>
    <span id="bwmde">
      <button {{action 'toggleTargetElement'}}>Do It</button>
    </span>
  </div>
  {{code-snippet name='target-element-modal-dialog.hbs'}}
  {{#if isShowingTargetElement}}
    {{!-- BEGIN-SNIPPET target-element-modal-dialog --}}
    {{#modal-dialog onClose='toggleTargetElement'
                    targetAttachment=exampleTargetAttachment
                    attachment=exampleAttachment
                    target="#bwmde"
                    aria-labelledby="example-target-element-title"
                    aria-describedby="example-target-element-desc" }}
      <h1 id="example-target-element-title">Stop! Modal Time!</h1>
      <p id="example-target-element-desc">Target - Element #bwmde</p>
      <p>Target Attachment: {{exampleTargetAttachment}}</p>
      <p>Attachment: {{exampleAttachment}}</p>
      <button {{action 'closeTargetElement'}}>Close</button>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-subclass'>
  <h2>Via Subclass</h2>
  <button onclick={{action (mut isShowingSubclassed) true}}>Do It</button>
  {{code-snippet name='subclass.js'}}
  {{code-snippet name='subclass-modal-dialog.hbs'}}
  {{code-snippet name='subclass-styles.scss'}}
  {{#if isShowingSubclassed}}
    {{!-- BEGIN-SNIPPET subclass-modal-dialog --}}
    {{#my-cool-modal-dialog
        onClose=(action (mut isShowingSubclassed) false)
        translucentOverlay=true
    }}
      <h1 id="my-cool-title">Stop! Modal Time!</h1>
      <p id="my-cool-desc">Via Subclass</p>
      <button onclick={{action (mut isShowingSubclassed) false}}>Close</button>
    {{/my-cool-modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>

<div class='example' id='example-in-place'>
<h2>In Place</h2>
  <button onclick={{action (mut isShowingInPlace) true}}>Do It</button>
  {{code-snippet name='in-place-modal-dialog.hbs'}}
  {{code-snippet name='in-place.scss'}}
  <div id='container-in-place'>
    I AM THE CONTAINER
    {{#if isShowingInPlace}}
      {{!-- BEGIN-SNIPPET in-place-modal-dialog --}}
      {{#modal-dialog
          onClose=(action (mut isShowingSubclassed) false)
          renderInPlace=true
          targetAttachment='none'
          containerClass='ember-modal-dialog-in-place'
          overlayClass='ember-modal-overlay-in-place'
          aria-labelledby="example-in-place-title"
          aria-describedby="example-in-place-desc"
      }}
        <h1 id="example-in-place-title">Stop! Modal Time!</h1>
        <p id="example-in-place-desc">In Place</p>
        <button onclick={{action (mut isShowingInPlace) false}}>Close</button>
      {{/modal-dialog}}
      {{!-- END-SNIPPET --}}
    {{/if}}
  </div>
</div>

<div class='example' id='example-centered-scrolling'>
  <h2>Centered Scrolling</h2>
  <button {{action 'toggleCenteredScrolling'}}>Do It</button>
  {{code-snippet name='centered-scrolling-modal-dialog.hbs'}}
  {{code-snippet name='centered-scrolling.scss'}}
  <div id='container-centered-scrolling'>
    {{#if isShowingCenteredScrolling}}
      {{!-- BEGIN-SNIPPET centered-scrolling-modal-dialog --}}
      {{#modal-dialog
          onClose='toggleCenteredScrolling'
          translucentOverlay=true
          targetAttachment='none'
          containerClass='centered-scrolling-container'
          overlayClass='centered-scrolling-overlay'
          wrapperClass='centered-scrolling-wrapper'
          aria-labelledby="example-centered-scrolling-title"
      }}
        <h1 id="example-centered-scrolling-title">Really Long Content To Demonstrate Scrolling</h1>
        <ul>
          <li>Hover over modal and scroll</li>
          <li>Also hover over overlay and scroll</li>
        </ul>
        {{lorem-ipsum length=30000}}
        <button {{action 'toggleCenteredScrolling'}}>Close</button>
      {{/modal-dialog}}
      {{!-- END-SNIPPET --}}
    {{/if}}
  </div>
</div>

<div class='example'>
  <h2>Element Center</h2>
  <span id='elementCenter'>
    <button onclick={{action (mut isShowingElementCenterModal) true}}>Do It</button>
  </span>
  {{code-snippet name='element-centered-modal-dialog.hbs'}}
  {{#if isShowingElementCenterModal}}
  {{!-- BEGIN-SNIPPET element-centered-modal-dialog --}}
    {{#modal-dialog
        onClose=(action (mut isShowingElementCenterModal) false)
        elementId=elementId
        translucentOverlay=true
        targetAttachment='elementCenter'
        target='#elementCenter'
        aria-labelledby="example-center-title"
    }}
      <p id="example-center-title">Centered on element.</p>
    {{/modal-dialog}}
    {{!-- END-SNIPPET --}}
  {{/if}}
</div>
